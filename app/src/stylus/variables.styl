// from gulpfile
$env = env

// color
$black = #333

// animation
$transitionTime ?= 0.3s

// sizes
$pcWidth = 1000px
$spWidth = 85%

$pcHeaderHeight = 60px
$pcFooterHeight = 30px
$pcMainMargin = 30px

// Responsive
$breakPointTablet = 768px